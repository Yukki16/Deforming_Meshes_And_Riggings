using System.Collections;
using System.Collections.Generic;
using ScreenSpaceBoolean;
using UnityEngine;

public class RigMesh : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
       /*foreach (var subtractee in Subtractee.GetAll()) {
            if(subtractee.GetComponent<MeshRenderer>() != null) {
                Destroy(subtractee.GetComponent<MeshRenderer>());
            }
            subtractee.
            SkinnedMeshRenderer smr = subtractee.AddComponent(typeof(SkinnedMeshRenderer)) as SkinnedMeshRenderer);
          // subtractee.GetComponent<MeshFilter>().sharedMesh;
        }*/
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
